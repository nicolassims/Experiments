<a href=<%= get_authorize_url(@conn, @request_token) %>>Authorize with Twitter</a>
<%= form_for @conn, Routes.auth_path(@conn, :auth), fn f -> %>
  <label>
   Twitter Pin: <%= text_input f, :pin %>
  </label>
  <%= hidden_input f, :request_token, value: @request_token %>
<% end %>
